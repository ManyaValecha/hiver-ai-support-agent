# Optional: set OPENAI_API_KEY to enable LLM-as-Judge evaluation
# Leave blank to use the built-in heuristic evaluator (no API needed)
# OPENAI_API_KEY=sk-...

# Flask secret key (only needed if you add session/cookie features)
# FLASK_SECRET_KEY=change-me-in-production
